import Logo from "../logo";
import Generations from "../generations";

const Header = () => {
  return (
    <div>
      <h2>This is header.</h2>
      <Logo />
      <Generations />
    </div>
  );
}

export default Header;

